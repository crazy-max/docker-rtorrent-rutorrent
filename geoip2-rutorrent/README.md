This directory is a vendored fork of the upstream ruTorrent GeoIP2 plugin.

For the original plugin documentation and upstream project history, see https://github.com/Micdu70/geoip2-rutorrent.

This repository keeps a local fork here so the image can build the plugin from source.
